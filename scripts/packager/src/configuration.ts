export enum PluginConfiguration {
  StandaloneRelease = "Release-Standalone",
  ThunderstoreRelease = "Release-Thunderstore",
  Debug = "Debug",
}

export enum TemplateConfiguration {
  Thunderstore = "thunderstore",
  Standalone = "standalone",
}

export enum PackageConfiguration {
  Thunderstore = "thunderstore",
  Standalone = "standalone",
  Dev = "dev",
}
